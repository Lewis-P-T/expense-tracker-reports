# Expense Tracker (Reports)

Menu-driven Python CLI for logging expenses by category. Data is saved to `expenses.json` next to the script. Standard library only.

```
python expense_tracker.py            # run the app
python expense_tracker.py --selfcheck
```

Work in progress — see `PROGRESS.md`.
